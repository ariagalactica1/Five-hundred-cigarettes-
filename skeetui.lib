-- GameSense UI - LocalScript
-- Place this in StarterPlayer > StarterPlayerScripts

local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

-- Create ScreenGui
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "GameSenseUI"
screenGui.ResetOnSpawn = false
screenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
screenGui.Parent = playerGui

-- Colors
local colors = {
	background = Color3.fromRGB(20, 20, 20),
	sideBar = Color3.fromRGB(15, 15, 15),
	panel = Color3.fromRGB(25, 25, 25),
	hover = Color3.fromRGB(35, 35, 35),
	green = Color3.fromRGB(140, 185, 50),
	text = Color3.fromRGB(200, 200, 200),
	textDim = Color3.fromRGB(120, 120, 120),
	border = Color3.fromRGB(40, 40, 40)
}

-- Main Frame
local mainFrame = Instance.new("Frame")
mainFrame.Name = "MainFrame"
mainFrame.Size = UDim2.new(0, 700, 0, 500)
mainFrame.Position = UDim2.new(0.5, -350, 0.5, -250)
mainFrame.BackgroundColor3 = colors.background
mainFrame.BorderSizePixel = 0
mainFrame.Parent = screenGui

-- Top Bar
local topBar = Instance.new("Frame")
topBar.Name = "TopBar"
topBar.Size = UDim2.new(1, 0, 0, 35)
topBar.BackgroundColor3 = colors.sideBar
topBar.BorderSizePixel = 0
topBar.Parent = mainFrame

local titleLabel = Instance.new("TextLabel")
titleLabel.Name = "Title"
titleLabel.Size = UDim2.new(0, 200, 1, 0)
titleLabel.Position = UDim2.new(0, 10, 0, 0)
titleLabel.BackgroundTransparency = 1
titleLabel.Text = "GAMESENSE"
titleLabel.TextColor3 = colors.text
titleLabel.TextSize = 14
titleLabel.TextXAlignment = Enum.TextXAlignment.Left
titleLabel.Font = Enum.Font.GothamBold
titleLabel.Parent = topBar

-- Side Navigation
local sideNav = Instance.new("Frame")
sideNav.Name = "SideNav"
sideNav.Size = UDim2.new(0, 120, 1, -35)
sideNav.Position = UDim2.new(0, 0, 0, 35)
sideNav.BackgroundColor3 = colors.sideBar
sideNav.BorderSizePixel = 0
sideNav.Parent = mainFrame

local navItems = {
	{name = "RageBot", icon = "🎯"},
	{name = "Anti Aim", icon = "🛡️", selected = true},
	{name = "LegitBot", icon = "🎮"},
	{name = "Players", icon = "👥"},
	{name = "Weapon", icon = "🔫"},
	{name = "Grenades", icon = "💣"},
	{name = "Bomb", icon = "🧨"},
	{name = "Visuals", icon = "👁️"},
	{name = "View", icon = "📷"},
	{name = "Main", icon = "⚙️"},
	{name = "Inventory", icon = "🎒"},
	{name = "Lua", icon = "📜"},
	{name = "Config", icon = "⚙️"}
}

for i, item in ipairs(navItems) do
	local btn = Instance.new("TextButton")
	btn.Name = item.name
	btn.Size = UDim2.new(1, -10, 0, 30)
	btn.Position = UDim2.new(0, 5, 0, (i-1) * 32 + 5)
	btn.BackgroundColor3 = item.selected and colors.panel or Color3.fromRGB(0, 0, 0, 0)
	btn.BackgroundTransparency = item.selected and 0 or 1
	btn.BorderSizePixel = 0
	btn.Text = "  " .. item.name
	btn.TextColor3 = item.selected and colors.green or colors.text
	btn.TextSize = 12
	btn.Font = Enum.Font.Gotham
	btn.TextXAlignment = Enum.TextXAlignment.Left
	btn.Parent = sideNav
	
	if item.selected then
		local indicator = Instance.new("Frame")
		indicator.Size = UDim2.new(0, 3, 1, 0)
		indicator.BackgroundColor3 = colors.green
		indicator.BorderSizePixel = 0
		indicator.Parent = btn
	end
end

-- Content Area
local contentArea = Instance.new("ScrollingFrame")
contentArea.Name = "ContentArea"
contentArea.Size = UDim2.new(1, -130, 1, -45)
contentArea.Position = UDim2.new(0, 125, 0, 40)
contentArea.BackgroundColor3 = colors.background
contentArea.BorderSizePixel = 0
contentArea.ScrollBarThickness = 6
contentArea.CanvasSize = UDim2.new(0, 0, 0, 800)
contentArea.Parent = mainFrame

-- Helper Functions
local function createSection(parent, title, yPos)
	local section = Instance.new("Frame")
	section.Name = title
	section.Size = UDim2.new(0, 250, 0, 30)
	section.Position = UDim2.new(0, 10, 0, yPos)
	section.BackgroundTransparency = 1
	section.Parent = parent
	
	local label = Instance.new("TextLabel")
	label.Size = UDim2.new(1, 0, 1, 0)
	label.BackgroundTransparency = 1
	label.Text = title
	label.TextColor3 = colors.text
	label.TextSize = 13
	label.Font = Enum.Font.GothamBold
	label.TextXAlignment = Enum.TextXAlignment.Left
	label.Parent = section
	
	return yPos + 30
end

local function createToggle(parent, name, enabled, xPos, yPos)
	local toggle = Instance.new("Frame")
	toggle.Name = name
	toggle.Size = UDim2.new(0, 240, 0, 25)
	toggle.Position = UDim2.new(0, xPos, 0, yPos)
	toggle.BackgroundTransparency = 1
	toggle.Parent = parent
	
	local label = Instance.new("TextLabel")
	label.Size = UDim2.new(0, 160, 1, 0)
	label.BackgroundTransparency = 1
	label.Text = name
	label.TextColor3 = colors.textDim
	label.TextSize = 11
	label.Font = Enum.Font.Gotham
	label.TextXAlignment = Enum.TextXAlignment.Left
	label.Parent = toggle
	
	local btn = Instance.new("TextButton")
	btn.Size = UDim2.new(0, 35, 0, 18)
	btn.Position = UDim2.new(1, -35, 0.5, -9)
	btn.BackgroundColor3 = enabled and colors.green or colors.panel
	btn.BorderSizePixel = 0
	btn.Text = ""
	btn.Parent = toggle
	
	local corner = Instance.new("UICorner")
	corner.CornerRadius = UDim.new(0, 4)
	corner.Parent = btn
	
	local indicator = Instance.new("Frame")
	indicator.Size = UDim2.new(0, 14, 0, 14)
	indicator.Position = enabled and UDim2.new(1, -16, 0.5, -7) or UDim2.new(0, 2, 0.5, -7)
	indicator.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
	indicator.BorderSizePixel = 0
	indicator.Parent = btn
	
	local indCorner = Instance.new("UICorner")
	indCorner.CornerRadius = UDim.new(0, 3)
	indCorner.Parent = indicator
	
	btn.MouseButton1Click:Connect(function()
		enabled = not enabled
		btn.BackgroundColor3 = enabled and colors.green or colors.panel
		indicator:TweenPosition(
			enabled and UDim2.new(1, -16, 0.5, -7) or UDim2.new(0, 2, 0.5, -7),
			Enum.EasingDirection.Out,
			Enum.EasingStyle.Quad,
			0.2,
			true
		)
	end)
	
	return yPos + 28
end

local function createDropdown(parent, name, value, xPos, yPos)
	local dropdown = Instance.new("Frame")
	dropdown.Name = name
	dropdown.Size = UDim2.new(0, 240, 0, 25)
	dropdown.Position = UDim2.new(0, xPos, 0, yPos)
	dropdown.BackgroundTransparency = 1
	dropdown.Parent = parent
	
	local label = Instance.new("TextLabel")
	label.Size = UDim2.new(0, 100, 1, 0)
	label.BackgroundTransparency = 1
	label.Text = name
	label.TextColor3 = colors.textDim
	label.TextSize = 11
	label.Font = Enum.Font.Gotham
	label.TextXAlignment = Enum.TextXAlignment.Left
	label.Parent = dropdown
	
	local btn = Instance.new("TextButton")
	btn.Size = UDim2.new(0, 130, 0, 20)
	btn.Position = UDim2.new(1, -130, 0.5, -10)
	btn.BackgroundColor3 = colors.panel
	btn.BorderSizePixel = 0
	btn.Text = value .. " ▼"
	btn.TextColor3 = colors.text
	btn.TextSize = 10
	btn.Font = Enum.Font.Gotham
	btn.Parent = dropdown
	
	local corner = Instance.new("UICorner")
	corner.CornerRadius = UDim.new(0, 3)
	corner.Parent = btn
	
	return yPos + 28
end

local function createSlider(parent, name, value, xPos, yPos)
	local slider = Instance.new("Frame")
	slider.Name = name
	slider.Size = UDim2.new(0, 240, 0, 35)
	slider.Position = UDim2.new(0, xPos, 0, yPos)
	slider.BackgroundTransparency = 1
	slider.Parent = parent
	
	local label = Instance.new("TextLabel")
	label.Size = UDim2.new(0, 160, 0, 15)
	label.BackgroundTransparency = 1
	label.Text = name
	label.TextColor3 = colors.textDim
	label.TextSize = 11
	label.Font = Enum.Font.Gotham
	label.TextXAlignment = Enum.TextXAlignment.Left
	label.Parent = slider
	
	local track = Instance.new("Frame")
	track.Size = UDim2.new(1, 0, 0, 4)
	track.Position = UDim2.new(0, 0, 0, 20)
	track.BackgroundColor3 = colors.panel
	track.BorderSizePixel = 0
	track.Parent = slider
	
	local fill = Instance.new("Frame")
	fill.Size = UDim2.new(0.6, 0, 1, 0)
	fill.BackgroundColor3 = colors.green
	fill.BorderSizePixel = 0
	fill.Parent = track
	
	local handle = Instance.new("Frame")
	handle.Size = UDim2.new(0, 10, 0, 10)
	handle.Position = UDim2.new(0.6, -5, 0.5, -5)
	handle.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
	handle.BorderSizePixel = 0
	handle.Parent = track
	
	local handleCorner = Instance.new("UICorner")
	handleCorner.CornerRadius = UDim.new(1, 0)
	handleCorner.Parent = handle
	
	return yPos + 38
end

-- Build Anti-Aim Content
local yOffset = 10

-- Anti-aimbot angles section
yOffset = createSection(contentArea, "Anti-aimbot angles", yOffset)
yOffset = createToggle(contentArea, "Enabled", true, 10, yOffset)
yOffset = createDropdown(contentArea, "Yaw base", "Local view", 10, yOffset)
yOffset = createSlider(contentArea, "Yaw", 0, 10, yOffset)
yOffset = createSlider(contentArea, "Yaw jitter", 0, 10, yOffset)
yOffset = createDropdown(contentArea, "Body yaw", "Off", 10, yOffset)
yOffset = createToggle(contentArea, "Freestanding body yaw", false, 10, yOffset)
yOffset = createToggle(contentArea, "Edge yaw", false, 10, yOffset)
yOffset = createDropdown(contentArea, "Freestanding", "", 10, yOffset)

yOffset = yOffset + 15

-- Fake lag section
local fakeLagY = 10
yOffset = createSection(contentArea, "Fake lag", fakeLagY)
fakeLagY = createToggle(contentArea, "Enabled", true, 270, fakeLagY)
fakeLagY = createDropdown(contentArea, "Amount", "Dynamic", 270, fakeLagY)
fakeLagY = createSlider(contentArea, "Variance", 0, 270, fakeLagY)
fakeLagY = createSlider(contentArea, "Limit", 0, 270, fakeLagY)

-- Other section
local otherY = fakeLagY + 15
yOffset = createSection(contentArea, "Other", otherY)
otherY = createToggle(contentArea, "Slow motion", false, 270, otherY)
otherY = createDropdown(contentArea, "Slow motion type", "Stand high speed", 270, otherY)
otherY = createDropdown(contentArea, "Leg movement", "Off", 270, otherY)
otherY = createToggle(contentArea, "On shot anti-aim", false, 270, otherY)
otherY = createToggle(contentArea, "Fake peek", false, 270, otherY)

-- Make draggable
local dragging = false
local dragInput, mousePos, framePos

topBar.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 then
		dragging = true
		mousePos = input.Position
		framePos = mainFrame.Position
	end
end)

topBar.InputEnded:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 then
		dragging = false
	end
end)

UserInputService.InputChanged:Connect(function(input)
	if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
		local delta = input.Position - mousePos
		mainFrame.Position = UDim2.new(
			framePos.X.Scale,
			framePos.X.Offset + delta.X,
			framePos.Y.Scale,
			framePos.Y.Offset + delta.Y
		)
	end
end)

print("GameSense UI Loaded")
